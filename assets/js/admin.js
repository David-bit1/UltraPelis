const adminApp = document.querySelector("#admin-app");

adminApp.insertAdjacentHTML(
  "beforeend",
  `<section class="panel">
    <h2>Formulario pendiente</h2>
    <p>En el siguiente paso se preparara el flujo para agregar peliculas al JSON.</p>
  </section>`,
);
